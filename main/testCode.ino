void testCode() {
  // openGripper();
  // delay(1000);
  // closeGripper();
  // delay(2000);

  // turnInfinite();

  // for (int i = 0; i < 4; i++) {
  //   drive();
  //   delay(2000);
  //   turnRight();
  //   delay(2000);
  // }

  // Serial.println("Close");
  // closeGripper();
  // delay(1000);
  // Serial.println("Open");
  // openGripper();
  // delay(1000);

  turnRight();
  delay(1000);
  stop();
  delay(1000);

  // ledBlink();

  // Serial.println(getDistance());
}
